// import type { ProtectedComponent } from '@solid-mediakit/auth/client'

// const Protected: ProtectedComponent = (session$) => {
//   return <h1>protected route - {JSON.stringify(session$)}</h1>
// }

// export default Protected
